<div class="max-w-3xl mx-auto p-4 sm:p-6 lg:p-8 bg-white shadow-xl rounded-xl">
  <div class="flex justify-between items-center mb-6 border-b pb-3">
    <h1 class="text-3xl font-extrabold text-gray-900"><%= @contact.name %> との着用履歴</h1>
    <%= link_to "会う人の詳細に戻る", contact_path(@contact), class: "text-indigo-600 hover:text-indigo-900 font-medium transition duration-150" %>
  </div>

  <p class="text-gray-600 mb-6">
    **<%= @contact.name %>** さんと会った時の、過去のアイテム着用実績一覧です。
  </p>

  <% if @history_outfits.any? %>
    <div class="space-y-6">
      <% @history_outfits.each do |outfit| %>
        <div class="bg-gray-50 p-4 rounded-lg shadow-sm border border-gray-200">
          <div class="flex justify-between items-start mb-2">
            <!-- 着用日と時間帯 -->
            <div class="text-lg font-semibold text-gray-800">
              <%= l(outfit.worn_on, format: :long) %>
              <span class="text-sm font-normal text-indigo-600 ml-2">
                (<%= ActualOutfitsController::TIME_SLOTS[outfit.time_slot] || '時間帯不明' %>)
              </span>
            </div>
            
            <!-- アクション -->
            <%= link_to "実績を編集/削除", actual_outfit_path(outfit), class: "text-sm text-gray-500 hover:text-gray-700 transition duration-150" %>
          </div>

          <!-- アイテム情報 -->
          <div class="flex items-center space-x-3 mt-1">
            <span class="text-xs font-medium text-gray-500">着用アイテム:</span>
            <span class="text-base font-bold text-gray-900">
              <%= outfit.item.name %>
            </span>
            <span class="text-sm text-gray-600">
              (<%= outfit.item.category.present? ? outfit.item.category : 'カテゴリー不明' %>)
            </span>
          </div>
          
          <!-- 感想/メモ -->
          <% if outfit.impression.present? %>
            <div class="mt-3 p-3 bg-white rounded border border-gray-100">
              <span class="text-xs font-medium text-gray-500">メモ・感想:</span>
              <p class="text-sm text-gray-700 whitespace-pre-wrap mt-1"><%= outfit.impression %></p>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  <% else %>
    <div class="p-6 bg-yellow-50 border border-yellow-200 rounded-lg">
      <p class="text-gray-700">
        まだ **<%= @contact.name %>** さんと会った時の着用実績は登録されていません。
        実績を登録する際に、会った人として **<%= @contact.name %>** さんを選択してください。
      </p>
    </div>
  <% end %>

  <div class="mt-8 text-center">
    <%= link_to "会う人一覧に戻る", contacts_path, class: "text-sm text-gray-500 hover:text-gray-700 font-medium transition duration-150" %>
  </div>
</div>