<%
  # --- ★修正: 色設定ロジック ---
  # まず「お気に入り(is_favorite)」かどうかを判定します。
  if @contact.is_favorite
    # お気に入りの場合の特別カラー（ピンク系）
    colors = { bg: 'bg-pink-100', border: 'border-pink-200', text: 'text-pink-500', label_bg: 'bg-pink-50', label_text: 'text-pink-600' }
  else
    # それ以外はカテゴリに基づいて色を決定
    colors = case @contact.category
    when 'family'  then { bg: 'bg-orange-100', border: 'border-orange-200', text: 'text-orange-500', label_bg: 'bg-orange-50', label_text: 'text-orange-600' }
    when 'friend'  then { bg: 'bg-lime-100', border: 'border-lime-200', text: 'text-lime-500', label_bg: 'bg-lime-50', label_text: 'text-lime-600' }
    when 'work'    then { bg: 'bg-sky-100', border: 'border-sky-200', text: 'text-sky-500', label_bg: 'bg-sky-50', label_text: 'text-sky-600' }
    # other または未設定(nil)の場合
    else                { bg: 'bg-stone-100', border: 'border-stone-200', text: 'text-stone-500', label_bg: 'bg-stone-50', label_text: 'text-stone-600' }
    end
  end
%>

<div class="min-h-screen bg-stone-50 flex items-center justify-center p-4 md:p-8 font-sans">
  
  <div class="w-full max-w-5xl bg-white rounded-[30px] shadow-2xl overflow-hidden flex flex-col md:flex-row min-h-[600px] relative">
    
    <%# --- 左側: プロフィールエリア --- %>
    <%# ★修正: border-dashed を削除し、リング穴と合わせるための border-r-0 に変更 %>
    <div class="w-full md:w-5/12 <%= colors[:bg] %> relative p-8 flex flex-col items-center justify-center text-center border-b-4 md:border-b-0 md:border-r-0 border-white/60 transition-colors duration-300 z-10">
      
      <%= link_to contacts_path, class: "absolute top-6 left-6 group flex items-center gap-2 px-4 py-2 bg-white/60 hover:bg-white rounded-full transition shadow-sm text-stone-500 hover:text-indigo-600" do %>
        <i class="fas fa-arrow-left"></i>
        <span class="text-xs font-bold">戻る</span>
      <% end %>

      <%# リング穴の装飾 %>
      <div class="hidden md:flex flex-col gap-6 absolute -right-5 top-1/2 -translate-y-1/2 z-20 pointer-events-none">
        <% 6.times do %>
          <div class="w-8 h-8 rounded-full bg-stone-50 border-[3px] border-stone-200 shadow-[inset_0_2px_4px_rgba(0,0,0,0.1)]"></div>
        <% end %>
      </div>

      <%# アバター %>
      <div class="relative mb-6 group mt-10 md:mt-0">
        <div class="w-40 h-40 md:w-48 md:h-48 rounded-full bg-white p-2 shadow-xl mx-auto relative">
          <div class="w-full h-full rounded-full overflow-hidden border-4 border-white bg-stone-50">
            <% if @contact.avatar.attached? %>
              <%= image_tag @contact.avatar.variant(resize_to_limit: [400, 400]), class: "w-full h-full object-cover" %>
            <% else %>
              <div class="w-full h-full flex items-center justify-center text-stone-300 text-5xl"><i class="fas fa-user"></i></div>
            <% end %>
          </div>
          
          <%# お気に入り星マークのデザイン調整 %>
          <% if @contact.is_favorite %>
            <div class="absolute -bottom-1 -right-1 bg-white text-yellow-400 w-14 h-14 rounded-full flex items-center justify-center shadow-md border-[5px] border-white text-3xl">
              <i class="fas fa-star drop-shadow-sm"></i>
            </div>
          <% end %>
        </div>
      </div>

      <h1 class="text-2xl md:text-3xl font-bold text-stone-700 mb-3 tracking-wider"><%= @contact.name %></h1>
      
      <%# カテゴリラベル %>
      <span class="inline-block px-5 py-1.5 rounded-full text-sm font-bold shadow-sm <%= colors[:label_bg] %> <%= colors[:label_text] %> mb-8">
        <%= @contact.category_label %>
      </span>
    </div>

    <%# --- 右側: 詳細エリア --- %>
    <div class="w-full md:w-7/12 bg-white p-8 md:p-12 relative flex flex-col">
      <%# ノート風の罫線背景 %>
      <div class="absolute inset-0 pointer-events-none opacity-10" style="background-image: linear-gradient(#9ca3af 1px, transparent 1px); background-size: 100% 2.5rem; margin-top: 4rem;"></div>
      
      <%# 左端のギザギザ（リングノートの端）を表現 %>
      <div class="hidden md:block absolute top-0 bottom-0 -left-2 w-4 bg-repeat-y z-0" style="background-image: radial-gradient(circle at 0 50%, transparent 0.4rem, #ffffff 0.5rem); background-size: 1rem 2rem;"></div>

      <div class="relative z-10 h-full flex flex-col">
        <div class="flex items-center gap-3 mb-10 border-b-2 border-stone-100 pb-3">
          <i class="fas fa-id-card text-stone-300 text-2xl"></i>
          <h2 class="text-lg font-bold text-stone-400 tracking-[0.2em]">PROFILE</h2>
        </div>

        <div class="mb-10 pl-2">
          <label class="block text-xs font-bold text-stone-400 mb-2">ひとことメモ</label>
          <p class="text-stone-700 font-bold text-xl font-sans leading-relaxed">
            <%= @contact.memo.presence || "（まだ書かれていません）" %>
          </p>
        </div>

        <div class="mb-10 pl-2">
          <label class="block text-xs font-bold text-stone-400 mb-2">最後に会った日</label>
          <div class="flex items-center gap-4">
            <% if @last_met_outfit.present? %>
              <span class="text-3xl font-bold text-stone-700 font-sans tracking-tight">
                <%= @last_met_outfit.worn_on.strftime("%Y.%m.%d") %>
              </span>
              <% if @last_met_outfit.items.present? %>
                <div class="bg-stone-100 px-3 py-1.5 rounded-lg text-xs text-stone-500 flex items-center gap-2 border border-stone-200">
                  <i class="fas fa-tshirt"></i>
                  <span class="max-w-[150px] truncate"><%= @last_met_outfit.items.first.name %></span>
                </div>
              <% end %>
            <% else %>
              <span class="text-stone-400 text-lg">まだ記録がありません</span>
            <% end %>
          </div>
        </div>

        <div class="mt-auto">
          <div class="bg-indigo-50 border-2 border-indigo-100 rounded-2xl p-4 flex flex-col sm:flex-row justify-between items-center gap-4 mb-6 shadow-sm">
            <div class="flex items-center gap-4">
              <div class="w-12 h-12 rounded-full bg-white flex items-center justify-center shadow-sm text-2xl">🍵</div>
              <div>
                <div class="text-xs font-bold text-indigo-400">会った/会う回数</div>
                <div class="text-xl font-bold text-indigo-900">
                  <span class="text-2xl"><%= @outfits.count %></span> 回
                </div>
              </div>
            </div>
            <button onclick="document.getElementById('history-modal').classList.remove('hidden')" class="w-full sm:w-auto bg-white hover:bg-indigo-500 hover:text-white text-indigo-600 border border-indigo-200 px-6 py-3 rounded-xl font-bold text-sm transition shadow-sm flex items-center justify-center gap-2 group">
              <i class="fas fa-list-ul group-hover:scale-110 transition"></i>
              <span>詳細リストを見る</span>
            </button>
          </div>

          <div class="grid grid-cols-2 gap-4">
            <%= link_to edit_contact_path(@contact), class: "bg-white border-2 border-stone-200 text-stone-600 rounded-xl py-3 font-bold text-sm hover:bg-stone-50 transition flex items-center justify-center gap-2" do %>
              <i class="fas fa-pen"></i> 編集する
            <% end %>
            <%= button_to contact_path(@contact), method: :delete, form: { data: { turbo_confirm: "本当に削除しますか？" } }, class: "w-full bg-white border-2 border-red-100 text-red-400 rounded-xl py-3 font-bold text-sm hover:bg-red-50 transition flex items-center justify-center gap-2" do %>
              <i class="fas fa-trash-alt"></i> 削除
            <% end %>
          </div>
        </div>
      </div>
    </div>
  </div>

  <%# --- モーダル（詳細リスト） --- %>
  <div id="history-modal" class="fixed inset-0 z-50 hidden" aria-labelledby="modal-title" role="dialog" aria-modal="true">
    <div class="fixed inset-0 bg-stone-900/60 backdrop-blur-sm transition-opacity" onclick="document.getElementById('history-modal').classList.add('hidden')"></div>
    <div class="fixed inset-0 z-10 overflow-y-auto">
      <div class="flex min-h-full items-center justify-center p-4 text-center sm:p-0">
        <div class="relative transform overflow-hidden rounded-3xl bg-white text-left shadow-2xl transition-all sm:my-8 sm:w-full sm:max-w-2xl border-4 border-indigo-50">
          
          <%# ヘッダー %>
          <div class="bg-indigo-50 px-6 py-4 flex items-center justify-between border-b border-indigo-100">
            <h3 class="text-lg font-bold text-indigo-900 flex items-center gap-2">
              <i class="fas fa-history text-indigo-400"></i> 会った時の記録(予定も含む)
            </h3>
            
            <%# 閉じるボタン %>
            <button type="button" 
                    onclick="document.getElementById('history-modal').classList.add('hidden')"
                    class="group bg-white hover:bg-stone-100 text-stone-500 rounded-full px-4 py-2 text-xs font-bold shadow-sm border border-stone-200 transition flex items-center gap-2">
              <i class="fas fa-times text-base"></i>
              <span>閉じる</span>
            </button>
          </div>

          <div class="px-6 py-6 max-h-[60vh] overflow-y-auto custom-scrollbar bg-stone-50/50">
            <% if @outfits.present? %>
              <div class="space-y-4">
                <% @outfits.each do |outfit| %>
                  <div class="bg-white rounded-2xl p-5 shadow-sm border border-stone-100 flex flex-col sm:flex-row gap-4">
                    <div class="flex sm:flex-col items-center justify-center gap-2 sm:w-24 border-b sm:border-b-0 sm:border-r border-stone-100 pb-2 sm:pr-4">
                      <div class="text-2xl font-bold text-stone-700"><%= outfit.worn_on.strftime("%m/%d") %></div>
                      <div class="text-xs font-bold text-stone-400"><%= outfit.worn_on.year %></div>
                    </div>
                    <div class="flex-1">
                      <div class="flex flex-wrap gap-3">
                        <% outfit.items.each do |item| %>
                          <div class="flex flex-col items-center gap-1 w-20">
                            <div class="w-20 h-20 rounded-xl bg-stone-100 border border-stone-200 overflow-hidden relative">
                              <% if item.image.attached? %>
                                <%= image_tag item.image.variant(resize_to_limit: [150, 150]), class: "w-full h-full object-cover" %>
                              <% else %>
                                <div class="w-full h-full flex items-center justify-center text-stone-300"><i class="fas fa-tshirt text-2xl"></i></div>
                              <% end %>
                            </div>
                            <span class="text-[10px] text-stone-500 font-bold truncate w-full text-center"><%= item.name %></span>
                          </div>
                        <% end %>
                      </div>
                    </div>
                  </div>
                <% end %>
              </div>
            <% else %>
              <div class="text-center py-12 text-stone-400">記録がありません</div>
            <% end %>
          </div>
          
          <%# フッター %>
          <div class="bg-gray-50 px-6 py-4 border-t border-stone-100 flex justify-end">
             <button type="button" onclick="document.getElementById('history-modal').classList.add('hidden')" class="bg-white hover:bg-stone-100 text-stone-600 border border-stone-300 rounded-lg px-8 py-2 text-sm font-bold shadow-sm transition">
              閉じる
            </button>
          </div>

        </div>
      </div>
    </div>
  </div>
</div>