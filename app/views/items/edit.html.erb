<div class="elegant-font py-10 px-4 sm:px-6 lg:px-8 max-w-4xl mx-auto">
  <div class="bg-white p-8 rounded-xl shadow-2xl border border-gray-100">

    <h1 class="text-3xl font-bold text-gray-800 border-b border-amber-100 pb-4 mb-8 text-center">
      アイテム情報編集
      <span class="block text-xl text-amber-700 mt-1">「<%= @item.name %>」</span>
    </h1>

    <%# ★修正箇所: form_with の二重囲みを削除%>
    <%# _form.html.erb 側ですでに form_with を定義しているため、ここでは呼び出すだけでOK%>
    <%= render 'form', item: @item %>

    <%# === 戻る・削除リンクエリア === %>
    <div class="mt-8 text-center flex justify-center items-center space-x-6 border-t border-gray-100 pt-6">
      <%= link_to "詳細画面に戻る", item_path(@item), class: "text-gray-600 hover:text-gray-900 text-sm font-medium transition duration-150" %>
      
      <span class="text-gray-300">|</span>

      <%# 削除ボタン (button_to で確実に動作) %>
      <%= button_to "このアイテムを削除", item_path(@item), method: :delete, form: { data: { turbo_confirm: '本当にこのアイテムを削除してもよろしいですか？' } }, class: "text-red-500 hover:text-red-700 text-sm font-medium transition duration-150 bg-transparent border-none cursor-pointer underline hover:no-underline" %>
    </div>
  </div>
</div>