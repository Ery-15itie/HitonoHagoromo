<div class="elegant-font py-10 px-4 sm:px-6 lg:px-8 max-w-7xl mx-auto">
  <div class="flex flex-col sm:flex-row justify-between items-center mb-8 border-b border-amber-100 pb-4">
    <h1 class="text-4xl font-bold text-gray-800">
      私のクローゼット
    </h1>
    
    <div class="flex flex-col sm:flex-row space-y-3 sm:space-y-0 sm:space-x-3 mt-4 sm:mt-0">
      <%# === 着用カレンダーへのリンク  === %>
      <%= link_to "着用カレンダーへ", actual_outfits_path, class: "flex items-center space-x-2 bg-amber-600 text-white py-2 px-4 rounded-xl font-semibold hover:bg-amber-700 transition duration-150 shadow-lg text-sm" %>
      
      <%# 新規アイテム登録ボタン %>
      <%= link_to new_item_path, class: "flex items-center space-x-2 bg-amber-800 text-white py-2 px-4 rounded-xl font-semibold hover:bg-amber-900 transition duration-150 shadow-lg text-sm" do %>
        <svg xmlns="http://www.w3.org/2000/svg" class="h-5 w-5" viewBox="0 0 20 20" fill="currentColor">
          <path fill-rule="evenodd" d="M10 5a1 1 0 011 1v3h3a1 1 0 110 2h-3v3a1 1 0 11-2 0v-3H6a1 1 0 110-2h3V6a1 1 0 011-1z" clip-rule="evenodd" />
        </svg>
        <span>新規アイテム登録</span>
      <% end %>
    </div>
  </div>

  <%# === アイテムリストの表示 === %>
  <% if @items.empty? %>
    <div class="text-center py-20 bg-gray-50 rounded-xl shadow-inner">
      <p class="text-xl text-gray-500 mb-4">まだアイテムが登録されていません。</p>
      <%= link_to "最初のアイテムを登録しましょう！", new_item_path, class: "text-amber-700 font-semibold hover:text-amber-900 transition duration-150" %>
    </div>
  <% else %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-8">
      <% @items.each do |item| %>
        <%# Item Card %>
        <div class="bg-white rounded-xl shadow-xl overflow-hidden hover:shadow-2xl transition duration-300 transform hover:scale-[1.02] border border-gray-100">
          
          <%# 画像の代わりにプレースホルダー %>
          <div class="h-48 bg-gray-200 flex items-center justify-center text-gray-500 font-medium text-sm border-b border-gray-300">
            <svg xmlns="http://www.w3.org/2000/svg" class="h-12 w-12 text-gray-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M4 3a2 2 0 00-2 2v10a2 2 0 002 2h12a2 2 0 002-2V5a2 2 0 00-2-2H4zm12 12H4l4-8 3 6 2-4 3 6z" clip-rule="evenodd" />
            </svg>
          </div>

          <div class="p-5">
            <%# アイテム名 %>
            <h2 class="text-xl font-bold text-gray-800 truncate mb-1">
              <%= link_to item.name, item_path(item), class: "hover:text-amber-800 transition duration-150" %>
            </h2>
            
            <%# カテゴリと価格 %>
            <div class="flex justify-between items-center text-sm text-gray-500 mb-3">
              <%# カテゴリが存在する場合のみバッジを表示。安全ナビゲーション演算子を使用 %>
              <% if item.category.present? %>
                <span class="bg-amber-100 text-amber-800 px-2 py-0.5 rounded-full font-semibold text-xs">
                  <%= item.category&.name %>
                </span>
              <% else %>
                <span class="text-xs text-gray-400 italic">
                  カテゴリなし
                </span>
              <% end %>

              <span class="font-medium">
                <% if item.price.present? %>
                  ¥<%= number_with_delimiter(item.price) %>
                <% end %>
              </span>
            </div>
            
            <%# 色と説明のプレビュー %>
            <p class="text-xs text-gray-500 truncate mb-4">
              色: <%= item.color.presence || '未設定' %>
            </p>
            
            <%# 詳細リンク %>
            <div class="text-center">
              <%= link_to "詳細を見る", item_path(item), class: "text-sm font-semibold text-amber-700 hover:text-amber-900 transition duration-150" %>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  <% end %>

</div>