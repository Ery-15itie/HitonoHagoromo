<%# --- Global Styles & Fonts Import --- %>
<%# しっぽり明朝フォントの読み込み %>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Shippori+Mincho:wght@600&display=swap" rel="stylesheet">

<%# 全体のラッパー：LPと同じ背景色とフォント設定 %>
<div class="min-h-screen bg-[#FDFBF7] text-slate-700 font-sans selection:bg-amber-100 selection:text-amber-900">
  
  <%# --- Header (LPスタイル) --- %>
  <header class="w-full py-6 px-6 flex justify-between items-center max-w-6xl mx-auto z-50 relative">
    
    <%# ロゴ部分: 👘 + しっぽり明朝 %>
    <%= link_to root_path, class: "flex items-center gap-3 text-indigo-950 hover:opacity-80 transition duration-300" do %>
      <span class="text-3xl filter drop-shadow-sm">👘</span>
      <span class="text-3xl font-bold tracking-widest" style="font-family: 'Shippori Mincho', serif;">
        ヒトノハゴロモ
      </span>
    <% end %>

    <%# ナビゲーション %>
    <nav class="flex items-center gap-6 text-sm font-bold text-indigo-900">
      <%# カレンダーへの正しいパスを設定 %>
      <%= link_to "カレンダー", actual_outfits_path, class: "hover:text-amber-700 transition" %>
      
      <%# 会う人の管理への正しいパスを設定 %>
      <%= link_to "会う人の管理", contacts_path, class: "hover:text-amber-700 transition" %>
      
      <%# アイテム一覧（現在地） %>
      <%= link_to "クローゼット", items_path, class: "text-amber-700 border-b-2 border-amber-700" %>
      
      <% if user_signed_in? %>
        <%= button_to "ログアウト", destroy_user_session_path, method: :delete, class: "bg-red-400 text-white px-4 py-2 rounded-full hover:bg-red-500 transition shadow-sm cursor-pointer border-none text-sm font-bold" %>
      <% else %>
        <%= link_to "ログイン", new_user_session_path, class: "bg-indigo-600 text-white px-4 py-2 rounded-full hover:bg-indigo-700 transition" %>
      <% end %>
    </nav>
  </header>

  <%# --- Main Content (Dashboard Layout) --- %>
  <main class="max-w-6xl mx-auto px-6 py-8">
    
    <%# フラッシュメッセージ (通知) %>
    <% if notice %>
      <div class="bg-green-100 border-l-4 border-green-500 text-green-700 p-4 mb-6 rounded shadow-sm" role="alert">
        <p class="font-bold">お知らせ</p>
        <p><%= notice %></p>
      </div>
    <% end %>
    <% if alert %>
      <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded shadow-sm" role="alert">
        <p class="font-bold">注意</p>
        <p><%= alert %></p>
      </div>
    <% end %>

    <div class="flex flex-col lg:flex-row gap-8">
      
      <%# --- Left Sidebar (検索・メニュー) --- %>
      <aside class="w-full lg:w-1/4 space-y-6">
        <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-100">
          <h2 class="text-xl font-bold text-indigo-950 mb-4" style="font-family: 'Shippori Mincho', serif;">私のクローゼット</h2>
          
          <%= link_to new_item_path, class: "block w-full text-center bg-amber-600 hover:bg-amber-700 text-white font-bold py-3 px-4 rounded-xl transition shadow-md mb-4" do %>
            + 新しく登録する
          <% end %>
          
          <%# ▼サイドバーのカレンダーリンクも修正しました %>
          <%= link_to actual_outfits_path, class: "block w-full text-center bg-white border-2 border-slate-200 text-slate-600 font-bold py-3 px-4 rounded-xl hover:bg-slate-50 transition" do %>
            📅 着用カレンダー
          <% end %>
        </div>

        <%# 並べ替え・フィルタ %>
        <div class="bg-white p-6 rounded-2xl shadow-sm border border-slate-100">
          <h3 class="font-bold text-slate-500 mb-4 text-sm">並べ替え</h3>
          <ul class="space-y-2 text-sm">
            <li><%= link_to "新しい順", "#", class: "block p-2 bg-amber-50 text-amber-800 font-bold rounded" %></li>
            <li><%= link_to "古い順", "#", class: "block p-2 text-slate-600 hover:bg-slate-50 rounded" %></li>
            <li><%= link_to "価格が安い順", "#", class: "block p-2 text-slate-600 hover:bg-slate-50 rounded" %></li>
            <li><%= link_to "価格が高い順", "#", class: "block p-2 text-slate-600 hover:bg-slate-50 rounded" %></li>
          </ul>
        </div>
      </aside>

      <%# --- Right Content (Grid Items) --- %>
      <div class="w-full lg:w-3/4">
        <div class="flex justify-between items-end mb-6">
          <h2 class="text-2xl font-bold text-indigo-950" style="font-family: 'Shippori Mincho', serif;">すべてのアイテム</h2>
          <span class="text-slate-500 text-sm">全 <%= @items.count %> 件</span>
        </div>

        <%# アイテムグリッド %>
        <div class="grid grid-cols-2 md:grid-cols-3 gap-6">
          <% @items.each do |item| %>
            <div class="group bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-md transition border border-slate-100 flex flex-col h-full">
              <%# 画像エリア %>
              <div class="relative aspect-[3/4] bg-slate-200 overflow-hidden">
                <% if item.image.attached? %>
                  <%= image_tag item.image, class: "w-full h-full object-cover group-hover:scale-105 transition duration-500" %>
                <% else %>
                  <div class="flex items-center justify-center h-full text-slate-400">
                    <span class="text-xs">画像なし</span>
                  </div>
                <% end %>

                <%# ホバー時の「詳細を見る」オーバーレイ %>
                <div class="absolute inset-0 bg-black/20 opacity-0 group-hover:opacity-100 transition flex items-center justify-center">
                  <%= link_to "詳細を見る", item_path(item), class: "bg-white text-xs font-bold px-4 py-2 rounded-full shadow-lg transform translate-y-2 group-hover:translate-y-0 transition" %>
                </div>
              </div>

              <%# テキスト情報 %>
              <div class="p-4 flex flex-col flex-grow">
                <h3 class="font-bold text-indigo-950 mb-1"><%= item.name %></h3>
                <div class="mt-auto">
                  <span class="inline-block bg-amber-100 text-amber-800 text-[10px] px-2 py-1 rounded-md font-bold">
                    <%= item.category.presence || "カテゴリなし" %>
                  </span>
                  <p class="text-xs text-slate-400 mt-2 text-right">
                    <%= item.created_at.strftime('%Y.%m.%d') %>
                  </p>
                </div>
              </div>
            </div>
          <% end %>
        </div>
        
      </div>
    </div>
  </main>
</div>