<!DOCTYPE html>
<html>
<head>
  <title>ヒトノハゴロモ</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  
  <%# Tailwind CSS CDNをロード %>
  <script src="https://cdn.tailwindcss.com"></script>

  <%# ヘルパータグ削除 %>
</head>

<body class="bg-gray-50">
  
  <%# ==== ナビゲーションバーの条件付き表示 ==== %>
  <%# ログイン済みのユーザーに対してのみ、アプリ内ナビゲーションを表示する %>
  <% if user_signed_in? %>
    <header class="bg-white shadow-md">
      <div class="max-w-7xl mx-auto py-4 px-4 sm:px-6 lg:px-8 flex justify-between items-center">
        <div class="text-2xl font-bold text-gray-800">
          <%= link_to "ヒトノハゴロモ", root_path %>
        </div>
        <nav>
          <%# ログイン後のナビゲーション %>
          <%= link_to "アイテム一覧", items_path, class: "text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md font-medium" %>
          <%= link_to "ログアウト", destroy_user_session_path, data: { "turbo-method": :delete }, class: "text-white bg-amber-800 hover:bg-amber-900 px-3 py-2 rounded-md font-medium ml-4" %>
        </nav>
      </div>
    </header>
  <% end %>

  <%# メインコンテンツエリア %>
  <main>
    <%= yield %>
  </main>
</body>
</html>